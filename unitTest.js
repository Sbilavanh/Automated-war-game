let expect = chai.expect;

describe("Function Deck", function () {
    it('This should create a 52 card deck' function () {
        let deck = []
        let start = 1
        expect(deck).to.be.a("Array");
        expect(start).to.equal(1);
    }
}
